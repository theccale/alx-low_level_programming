#include "main.h"

/**
 * -mul - multiplies two integers
 * @a: The first number to be multiplied
 * @b: The second number to be multiplied with the first
 *
 * Return: The result of the multiplication between the numbers
 */

int mul(int a, int b)
{
	return (a * b);
}
